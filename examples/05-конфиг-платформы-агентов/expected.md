# Ожидаемый отчёт

1. `yandex-deepseek-v4-flash` берёт ключ из `NOVITAAI_API_KEY`, а не из
   yandex-ключа — модель ходит на `ai.api.cloud.yandex.net`, ключ явно от
   другого провайдера · правило `П2`
2. `[models.openrouter-deepseek-v4-flash]` объявлен, но ни одна функция его
   не вызывает — лишний блок в конфиге · правило `П3`

НЕ нарушение: все переменные `toml заведены в секретах` — правило `П1`
соблюдено, сервис стартует.
НЕ нарушение (только вопрос на сверку): значения `cost_per_million`
проставлены вручную — правило `П5`, цены не проверяются автоматически,
только помечаются на ручной глаз.
