# Ожидаемый отчёт

Нарушение: Модель объявлена, но не используется ни одной функцией (П3).

Место: models.ghost-unused-model.

Цитата: `[models.ghost-unused-model]`

Что не так: блок модели добавлен, но ни функция его не вызывает — лишний блок в конфиге, ровно тот тип проблемы, что был в реальном инциденте заказчика с лишним параметром. Правило П3.

Ключевая проверка: инструмент должен указать именно это место и процитировать именно это значение — не общий «конфиг не соответствует».
